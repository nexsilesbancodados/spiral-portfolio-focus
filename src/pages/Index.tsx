const Index = () => {
  return (
    <div className="min-h-screen bg-background flex items-center justify-center">
      <p className="text-muted-foreground text-lg">Página em branco — pronta para construir.</p>
    </div>
  );
};

export default Index;
